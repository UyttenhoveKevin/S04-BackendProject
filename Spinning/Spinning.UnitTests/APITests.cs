using Microsoft.AspNetCore.Mvc;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using Moq;
using Spinning.API.Controllers;
using Spinning.API.DTOs;
using Spinning.Models;
using Spinning.Models.Repositories;
using System;
using System.Collections.Generic;
using System.Text;
using System.Threading.Tasks;

namespace Spinning.UnitTests
{
    [TestClass]
    public class APITests
    {
        //private Task<List<Timeslot>> timeslot;
        //[TestMethod]
        //private async Task GetTimeSlot_Returns_Value()
        //{
        //    var mockTimeSlot = new Mock<ITimeSlotRepository>();
        //    mockTimeSlot.Setup(t => t.GetAllAsync()).Returns(timeslot);

        //    var controller = new TimeslotsController(mockTimeSlot.Object);

        //}
    }
}
